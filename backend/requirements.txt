fastapi==0.115.*
uvicorn[standard]==0.32.*
sqlalchemy==2.0.*
alembic==1.13.*
pydantic==2.9.*
pydantic-settings==2.6.*
python-multipart==0.0.*
ollama==0.4.*
# RAG: SQLite FTS5 is built into stock Python -- no chromadb / sentence-transformers / torch.
# pymupdf stays for the upload-manual endpoint and the WHO IMGS preprocessor.
pymupdf==1.24.*
firebase-admin==6.6.*
python-dotenv==1.0.*
httpx==0.28.*
google-genai>=1.5
# Fleet Mechanic — no heavyweight deps; uses httpx (already above) for Phoenix REST calls
